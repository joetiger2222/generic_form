import { Routes } from '@angular/router';
import { PromoFormComponent } from './pages/promo/promo-form/promo-form.component';

export const routes: Routes = [
    {path:'',component:PromoFormComponent},
];
